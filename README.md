OpenAI sdk

calling LLM using model = gpt-5-mini give response based on my input
response looks like :- 
Fun fact: Data engineers are the invisible plumbers of analytics—building reliable pipelines and ETL so dashboards just work.  
A single production pipeline often moves terabytes overnight, so "data freshness" is a real engineering feat.
